# V3.7 Goal 3B Host Bridge Exceptional Template-Prompt Correction Prompt

Resume `/root/v37_g3b_host_port_bridge_implementation` under
`V3_7_G3B_HOST_EXECUTION_PORT_BRIDGE_EXCEPTIONAL_TEMPLATE_PROMPT_CORRECTION_AMENDMENT.md`.

Preserve without amend:

- failed initial Candidate `73b8b9787dea1ec2cb4d43be547aef711f74fbb3`;
- ordinary Correction 1 Candidate `e154d788ec2b3def81b50215fc732709132da517`.

Implement only `G3B-HOST-BRIDGE-MAIN-P1-002` within the same three paths. Pass the single
exact Host-loaded Manifest template into the Candidate port and include its ID and content
in the model-visible prompt. Validate the frozen ID/content/SHA-256 before construction.
Update the fake so its positive proposal comes from the template captured in the actual
request; assert exact prompt presence and retain all existing negative tests.

Do not alter configuration, V3.6, product source, Candidate semantics, budgets, ports,
control state or prior reports. Use no Credential/environment-secret reads, network,
Provider/model calls, Docker or Pi execution. Do not touch the user-owned untracked
evidence-audit report.

Run only bridge focused, authority focused, strict TypeScript and integrity checks. Update
the implementation report, create one exceptional corrected Candidate commit, and return
to Main. Do not start audit or real execution.
