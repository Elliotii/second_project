# V3.6 Goal 2 — Dedicated Implementation Session Start Prompt

You are the new top-level Implementation Session for:

```text
V3_6_G2_BOUNDED_EXECUTION_CHANGE_HANDOFF_AND_PRODUCT_ACCEPTANCE
```

Start only from this exact Control Baseline:

```yaml
git_commit: 992f721c4f05b7c78761966c7b8f79a6b4b3a2d3
git_tree: be96881831af1355cabdf4ffb0a617ed2bc35159
pinned_pi_commit: 027a5847901b5dde30270abaa1041046cd2b4b55
docker_desktop: 4.85.0_235549
docker_client_server: 29.6.2_29.6.2
docker_context: desktop-linux
docker_image: node@sha256:3638d9a6fe4030bd716be989438248074489337ba3275657f93595428be4fc03
```

## 1. Gate A and required reading

Before editing, verify exact HEAD/tree and clean tracked status, then read completely in Contract order:

- `AGENTS.md`;
- `CURRENT_STATE.md`;
- `docs/第二项目_Codex交接包_2026-07-30/V3_6_CHARTER.md`;
- `docs/第二项目_Codex交接包_2026-07-30/V3_6_G2_GOAL_CONTRACT.md`;
- `docs/reports/V3_6_DOCKER_READINESS_REPORT.md`;
- Goal 1 Closeout and Implementation Report;
- accepted V3.6 Planning, Backend Selection and Long-running Plan;
- relevant V3/V3.5/Post-V3.5 source and tests;
- applicable pinned Pi instructions/public exports before Pi claims.

Record Gate A under ignored `.runs/v3-6/g2/`. The Docker executable for this host is:

```text
C:/Users/HUAWEI/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe
```

This is Host-only configuration. Do not project the path to Browser/model/safe Session/evidence. Verify the exact digest image is already local and use `--pull never`; do not pull or select another image.

## 2. Authority

Implement the complete deterministic Goal 2 scope in the accepted Contract:

- one thin Docker registered-command executor;
- frozen backend/profile and immutable terminal evidence;
- bounded-edit tools only in managed Session copy;
- initial/final inventory and immutable ChangeSet blobs;
- safe Files/Changes/Diff/backend views;
- Host-only Apply All, Discard and Export with fail-closed preflight;
- truthful partial-apply receipt/recovery material;
- one-successful-Apply Session terminal rule;
- additive bilingual WebUI;
- the single dependency-free representative fixture;
- focused tests, raw evidence, report and Closeout Draft.

You may use Docker only for deterministic Contract tests with runtime `--network none`. Credential reads, external Provider/model calls and real-model calls are exactly zero. Do not perform the real two-Turn Journey.

## 3. Binding implementation rules

- Browser/model supplies only exact safe fields and registered command ID; Host owns argv, image, mount, env, profile and budget.
- `spawn` uses `shell:false`; there is no Host command fallback or second backend.
- The single mounted path is the canonical link-free managed copy at `/workspace`; never mount Source, `.git`, project `.runs`, home, Credential, State, Verifier, Authority or Docker socket.
- Persist Authority before execution and persist truthful create/start/output/inspect/timeout/kill/remove evidence.
- Enforce the exact Profile and image in the Contract.
- Agent/container/browser never write registered Source. Apply accepts only persisted identity plus explicit action and revalidates every invariant.
- Preserve all accepted V0–V3.5 behavior. Prefer V3.6 adapters. The optional bounded-tool executor seam must remain default-compatible and pass affected regressions.
- Normal TypeScript/Docker argv/path/fixture/HTTP/UI defects are yours to fix in scope. Stop only for a Contract Hard Stop.

## 4. Files, commit and stop

Modify only the Contract allowlist. Do not modify or stage `CURRENT_STATE.md`, `AGENTS.md`, Charter, Contract, accepted Closeouts, Pi, Credential or tracked reference files.

Run the narrowest focused tests while developing, then all Contract-required strict TypeScript and affected regressions. Prove exact Docker cleanup and zero leftover Goal containers. Run the existing secret scan on the final tracked delta.

Create exactly one bounded implementation commit on your task worktree. Return:

- exact commit/tree and parent baseline;
- `V3_6_G2_IMPLEMENTATION_REPORT.md`;
- `V3_6_G2_CLOSEOUT_DRAFT.md`;
- exact commands, exit codes and test counts;
- ignored Evidence Index/digests;
- Source Delta;
- remaining limitations;
- structured `CURRENT_STATE_UPDATE_PROPOSAL`.

Then stop. Do not accept Goal 2/V3.6, create an Execution Baseline, read Credential, call a model or start the real Journey.
