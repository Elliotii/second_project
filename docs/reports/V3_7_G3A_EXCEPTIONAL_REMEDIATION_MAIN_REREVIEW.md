# V3.7 Goal 3A Exceptional Remediation Main Rereview

## Disposition

- **Fact:** `PASS_V3_7_G3A_EXCEPTIONAL_REMEDIATION_MAIN_REREVIEW`.
- **Fact:** Reviewed Candidate commit `f82a65612ef66b21f65c110fcdc4feee91907c74`, tree `04d73a7714f007a07ad7c9825dad02ca90267c51`, parent dispatch baseline `aa3f13ffa98eeb402dc4d29411cf63a8498b33ea`.
- **Fact:** This is the sole Candidate authorized by the accepted one-time remediation exception. The ordinary correction budget remains exhausted at `2/2`.
- **Fact:** Goal 3A is not yet accepted. Goal 3B and all real access remain locked pending independent focused-audit PASS and Main integration.

## Contract rereview

- **Fact:** Primary routing now derives `no_recovery_needed`, `ready_for_recovery`, or the Charter-frozen empty-action `recovery_inconclusive` from independently inspected formal terminal facts. Invalid combinations receive no successful receipt.
- **Fact:** A real-declared Primary requires an injected, real-authorized V2 episode with the exact Host-loaded Task identity and counter tuple; a G3A-local terminal cannot satisfy that route. The Recovery bridge reuses the same declaration while retaining V2 Task, Verifier, strategy, budget, Base-Prompt and Candidate-Path checks.
- **Fact:** Product construction now carries Primary, Candidate-proposal, Regression-validation and bound follow-up Runtime ports. Candidate and Regression authority digests derive from Host-loaded Manifest specs.
- **Fact:** Every mutating `act()` reloads the workflow registration and requires matching construction authorization, all four real-declared ports and a non-disabled registration before dispatch. Ports-only restart cannot mutate any later stage; read-only reopen remains available.
- **Fact:** The selected simulation reaches retained Assessment through the accepted V2, G1, producer, symmetric comparator, State CAS, V3.6 Runtime/Verifier/Outcome, second admission and G2 handlers. It does not replace them with directly manufactured product artifacts.

## Original four-check reconciliation

1. **Primary Run identity:** workflow registration, pre-execution Run binding, Task instance, run root, returned/stored terminal and inspected V2 identity are checked together before a receipt and again on reopen.
2. **Registry baseline:** Host registry authority remains anchored to the canonical Host project root; caller-selected alternate roots continue to fail the accepted G1/G3A authority suites.
3. **Frozen content independence:** Primary preflight independently hashes and cross-checks the frozen Task, instruction, Source tree and Verifier before dispatch; drift variants fail with zero receipt.
4. **Disabled historical workflow:** disabled registration blocks mutation while the exact accepted workflow and artifacts reopen read-only and remain inspectable.

## Main verification

All suites were run serially from `workbench/`.

| Verification | Main result |
|---|---|
| G3A authority/Product/HTTP | PASS, 20/20 |
| accepted G1/G2 | PASS, 25/25 |
| V2 | PASS, 11/11 |
| V3 | PASS, 19/19 |
| V3.6 Product/HTTP/change handoff | PASS, 10/10 |
| strict TypeScript via authorized existing compiler | PASS, zero diagnostics |
| exact `npm run typecheck` wrapper | unavailable because its fixed ignored worktree compiler path is absent |
| G3A demo smoke | PASS; loopback start/stop, project commands `0`, Docker product commands `0` |
| Candidate parent/tree, one-commit rule, allowlist and `diff --check` | PASS |
| accepted v1 inventory and tracked config/fixture preservation | PASS |

- **Fact:** Formal nonzero counters are local simulation evidence. Source inspection and the deterministic mocks show actual Credential reads, network calls, Provider calls and model calls were all `0`.
- **Fact:** The user-owned untracked `docs/reports/SECOND_PROJECT_CASE_EVIDENCE_AUDIT.md` remains outside the Candidate and Main control commit.

## Scope classification

- **Fact:** The pre-existing Host-only `regressionCandidatePass` deterministic rejection switch remains outside HTTP, Case, workflow and Artifact authority. The frozen real-declared selected route omits that switch and uses the Host-supplied Regression validation port.
- **Recommendation:** Treat behavior under an intentionally altered future Host-only test construction as out of the frozen Goal 3A/Goal 3B execution configuration, not as a new remediation contract.
- **Fact:** No new issue meets the accepted must-fix threshold: the frozen path is not blocked, no false successful terminal or irreversible mutation was found, the core reusable-path claim remains true, and all explicit regressions pass.

## Audit freeze recommendation

- **Recommendation:** Freeze commit `f82a65612ef66b21f65c110fcdc4feee91907c74` / tree `04d73a7714f007a07ad7c9825dad02ca90267c51` as the immutable Goal 3A focused-audit Candidate.
- **Recommendation:** A fresh independent read-only audit should recheck the repeated P1-001 class, the four original checks, all three Primary routes, all four construction ports, per-action restart authority, complete selected simulation, historical reopen, hash/allowlist preservation and zero actual external access.
