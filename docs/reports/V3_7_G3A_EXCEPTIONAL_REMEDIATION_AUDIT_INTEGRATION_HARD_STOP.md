# V3.7 Goal 3A Exceptional Remediation Audit Integration Hard Stop

## Disposition

- **Fact:** Independent focused audit returned `FAIL_V3_7_G3A_EXCEPTIONAL_REMEDIATION_FOCUSED_AUDIT` for Candidate `f82a65612ef66b21f65c110fcdc4feee91907c74` / tree `04d73a7714f007a07ad7c9825dad02ca90267c51`.
- **Fact:** Main checked the cited ProductService branch and independently reran the audit-local deterministic reproduction with a fresh Case/workflow identity.
- **Fact:** Main reproduction PASS 1/1: the real-declared workflow reached `candidate_rejected` while the exact supplied Regression validation port was called `0` times.
- **Fact:** The test restored the tracked registry in `finally`; Candidate source, tests, configuration and control state were not changed.

## Main finding integration

- **Fact:** `V37-G3A-EXAUDIT-P1-001` is accepted as blocking. It is inside the repeated authority/terminalization class because an undigested Host test switch can substitute the required real-declared Regression execution port and determine a formal terminal.
- **Fact:** `V37-G3A-EXAUDIT-P2-002` is accepted as a required proof correction: the existing zero-valued object is not connected to the local mocks and therefore is not independent instrumentation.
- **Inference:** The P1 meets the accepted must-fix threshold: it makes the frozen reusable real-declared execution-boundary claim false. This is not an optional production-hardening improvement.

## Budget and authority

- **Fact:** Ordinary Goal 3A correction budget remains exhausted at `2/2`.
- **Fact:** The accepted exceptional-remediation budget and its single-Candidate authority are consumed by Candidate `f82a656...`.
- **Fact:** The Amendment explicitly makes audit failure or a second remediation Candidate a Hard Stop.
- **Fact:** Main therefore cannot repair, create another Candidate, accept Goal 3A or unlock Goal 3B without a new user decision and corresponding Charter amendment.

## Required user decision

- **Recommendation:** If continuation is desired, authorize one final audit-remediation amendment limited to:
  1. making real-declared Regression always use the exact supplied Regression port while preserving the deterministic frozen-Case rejection test path;
  2. connecting actual-operation instrumentation to all four local mocks; and
  3. the minimum focused test/report changes, one Candidate, Main rereview and independent re-audit.
- **Recommendation:** Otherwise retain the Hard Stop and leave Goal 3A unaccepted and Goal 3B locked.

