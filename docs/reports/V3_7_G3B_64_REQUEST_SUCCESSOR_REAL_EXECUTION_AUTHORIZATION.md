# V3.7 Goal 3B 64-Request Successor Real Execution Authorization

```yaml
status: AUTHORIZED_BY_USER_2026_08_21
execution_baseline_commit: 1a542e081420b1c0037a54bbcd641c329cfe70e2
execution_baseline_tree: 5280a9cca668ef7b81376e5f092aea3b31b75f88
execution_prompt: V3_7_GOAL_3B_64_REQUEST_SUCCESSOR_EXECUTION_PROMPT.md
execution_prompt_git_blob: 5577fa91bc450132e35c84a76ddbdcc8ce6f20c2
execution_prompt_sha256: e4f0c57629a8c82e1b2dbd0badcd85a904e5d59096e131a6ee060f322498f9ba
successor_execution_identity: v37-g3b-64-request-successor-v1
provider: deepseek
model: deepseek-v4-flash
opaque_credential_resolution_authorized: true
campaign_cost_usd_hard_max: 10.00
continuous_execution_boundary: stop_before_user_unguided_webui_check
```

## Authorized boundary

The user explicitly authorizes the frozen successor real execution, Host-side DeepSeek
traffic, one opaque Credential lease per versioned execution identity, the registered
Docker `network=none` command path, and continuous Main control through truthful real
execution, Main review, closeout preparation and read-only reopen. Execution must stop
immediately before the user's unguided WebUI check.

The existing Host Credential file may be supplied only as an opaque path to
`createDeferredCredentialFileResolverV35`. The parent shell and Main must not open,
print, hash, size, copy or summarize its contents, and must not enumerate environment
secrets. Raw Provider payloads, model text, headers and Credential values remain
forbidden in reports and user output.

The cumulative actual or fail-closed accounted cost of this authorized campaign must not
exceed USD 10.00. The frozen per-unit and bridge limits remain enforced inside each
execution identity and are ceilings, not targets.

## Versioned structural-correction route

An honest Case negative is final for its identity and must not be retried, replaced or
result-hunted. If execution instead proves a structural product defect that meets the
accepted must-fix rule, Main may preserve that identity and evidence, create the smallest
versioned correction and necessary narrow deterministic verification, perform the
required risk-based review/audit, freeze a new baseline and continue under a new
execution identity. Internal correction, commit and identity counts are not hard
completion limits; every change remains additive, auditable and within the USD 10.00
campaign cap.

The must-fix rule is limited to a defect that blocks the frozen Goal 3B path, risks data
damage or an incorrect-success state, invalidates a frozen core capability claim, or is
an explicit current acceptance regression. Other observations are deferred, out of
scope or optional. No correction may widen the product contract, introduce a new
subsystem, access unrelated data or weaken secret, evidence, budget or one-shot rules.

## Session ownership

The user directed the authoritative successor Main Session to plan and execute
continuously. Main therefore performs the fresh no-source-edit real execution in an
isolated worktree at the exact Execution Baseline and records this explicit owner
deviation. Source or control changes, if a structural correction becomes necessary,
occur only after the failed execution identity is closed and preserved.
