# V3.7 Goal 3B 64-Request Successor V2 Execution Prompt

> Status: `FROZEN_PENDING_EXACT_EXECUTION_BASELINE_RECORD`.

The authoritative successor Main performs this fresh no-source-edit execution in an
isolated worktree under the user's campaign authorization. Stop before workflow creation
or dispatch unless Main has recorded affected-finding audit PASS and supplied the exact
Execution Baseline commit/tree containing this Prompt.

## 1. Exact authority and Gate H

Read `AGENTS.md`, `CURRENT_STATE.md`, `V3_7_CHARTER.md`,
`V3_7_G3B_64_REQUEST_SUCCESSOR_V2_CONTRACT.md`, the Identity 1 execution report, the
structural-correction amendment/report/Main review, the affected-finding audit and this
Prompt.

```yaml
execution_identity: v37-g3b-64-request-successor-v2
configuration_candidate_commit: ca33885f4691f00ab9ab90643f8ed5fbc0825bb8
configuration_candidate_tree: ae12fd560f1d6dc802648a682eb5d94f96335f80
corrected_candidate_commit: 8a246e439da6aea1f597eed8e4ab71e57dc1b500
corrected_candidate_tree: c177337c39aba11ec180a4f85fa31437406ea82e
execution_baseline_commit: SELF_RESOLVED_BY_MAIN_FREEZE_RECORD
execution_baseline_tree: SELF_RESOLVED_BY_MAIN_FREEZE_RECORD
```

Verify a clean tracked tree, exact HEAD/tree, Candidate ancestry, Prompt Git blob/SHA-256
and these frozen blobs before any real boundary:

| Frozen path | Git blob |
|---|---|
| real Manifest v2 | `f8caccb526fd32af0cb02bbd62659707c594cdae` |
| accepted Envelope v2 | `bafb6148785f80dd9ae7179c6429b2519228dd3d` |
| follow-up profile v2 | `3813f7b06cffa3ca9d563b7a8aaf3070fe09c621` |
| canonical registry v2 | `4aaaedfeb741a86bd51f33e11f6e836222f42b6f` |
| corrected Host bridge | `77ac7307f2b84508b270e8e77f223c725d3087de` |
| corrected Primary Inspector | `b59163fb72b9fcb2c1514e44b4f76aad1164542b` |

Repeat the accepted local Docker identity and zero-leftover checks without pulling an
image or external Docker network access. Any mismatch is a zero-call stop.

## 2. Frozen Case and execution budgets

Case, Project, content digests, Candidate template, Provider/model and two Runtime
compositions are identical to Identity 1. Use `deepseek` / `deepseek-v4-flash`, one
opaque Credential lease and only `createRealExecutionPortBridgeV37G3B` plus
`createDeferredCredentialFileResolverV35`.

Primary/Recovery A/Recovery B each have request/token/Tool/registered-command/wall/USD
maxima `64/524288/96/64/3600000/0.20`. Candidate proposal remains
`1/16384/0/0/120000/0.20`. Regression Base/Candidate and follow-up each retain 64
requests, 524288 tokens, 96 Tools, one command, 3600000 ms and USD 0.20. Global bridge
maxima are `385/3162112/576/195/21720000/1.40`. Campaign cumulative cost must remain
below USD 10.00, including Identity 1 USD `0.0030990008`.

## 3. One-shot sequence

Create one new ignored root and one new workflow. Assert the exact two Runtime
compositions, empty completed units, null in-flight reservation and zero counters before
workflow creation. Invoke each available action at most once in order:

```text
run_primary
run_recovery
confirm_recovery_evidence
request_recovery_admission
produce_candidate
run_regression
run_follow_up
confirm_follow_up_evidence
request_follow_up_admission
assess_state
```

If Primary returns `no_recovery_needed`, stop successfully at that honest terminal; do
not force Recovery. Otherwise stop at the first unavailable, negative or invalid stage.
Persist only sanitized IDs, stages, counters, digests and receipts. Never persist or
print Credential values, headers, raw Provider payloads, model text or environment
contents.

After dispatch or any unit artifact, no retry, fallback, replacement, same-identity
rerun, task swap, Candidate reproposal or result hunting is allowed. Identity 1 must not
be opened or reused by this execution.

## 4. Return boundary

Close the bridge, return one truthful execution report to Main and preserve the isolated
ignored evidence. After Main review, reopen the same workflow through a new bridge whose
resolver throws on use. Stop immediately before the user's unguided WebUI check.
