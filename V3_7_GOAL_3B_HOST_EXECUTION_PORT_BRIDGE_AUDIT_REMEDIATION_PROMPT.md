# V3.7 Goal 3B Host Execution-Port Bridge Audit Remediation Prompt

Resume `/root/v37_g3b_host_port_bridge_implementation` under
`V3_7_G3B_HOST_EXECUTION_PORT_BRIDGE_AUDIT_REMEDIATION_AMENDMENT.md`.

Preserve Candidate `c85011fd9ea8a64d6b7964dd853750b13f2b2fa4` / tree
`2bb2fb399cd4f08a89da9c02edb3abf15e740938` unchanged. Close only audit findings
`V37-G3B-BRIDGE-AUDIT-P1-001` and `V37-G3B-BRIDGE-AUDIT-P1-002` within the same three
paths.

Implement a synchronous in-flight unit/group reservation before dispatch and a
Promise-coalesced shared Credential lease. Concurrent duplicate actions must fail before
Credential resolution, Runtime construction, Provider dispatch, Tool/command side effect
or receipt. Primary/Recovery remains one reserved execution group that completes its
three ordered logical units.

Do not change the public V2B seam. Record and inspect exactly two pinned Runtime
compositions: public V2B Primary/Recovery and bridge Post-V3.5 later stages, both DeepSeek
`deepseek-v4-flash`, with one Credential resolver invocation and one aggregate ledger.
Update the report truthfully.

Add local barrier-based concurrency regressions without real access. Preserve all eight
existing bridge checks and six authority checks. Run only those focused tests, strict
TypeScript and integrity checks. Use zero Credential/environment-secret reads, network,
Provider/model calls, Docker and Pi. Do not touch the user-owned untracked evidence-audit
report. Create one remediation Candidate and return to Main; do not audit or authorize
real execution.
